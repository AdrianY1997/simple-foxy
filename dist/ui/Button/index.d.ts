import React from "react";
import { OverridableFoxyButton } from "../../types/button";
declare const Button: ({ size, variant, color, startIcon, endIcon, children, transform, rounded, border, ...args }: OverridableFoxyButton) => React.JSX.Element;
export default Button;
