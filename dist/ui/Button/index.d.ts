import React from "react";
import { FoxyComponentOverridableButton } from "../../types/button";
declare const Button: ({ elevation, border, size, weight, rounded, transform, decoration, variant, color, children, ...args }: FoxyComponentOverridableButton) => React.JSX.Element;
export default Button;
