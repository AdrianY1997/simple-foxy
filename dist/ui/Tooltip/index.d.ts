import React from "react";
import { OverridableTooltipType } from "../../types/tooltip";
declare const Tooltip: ({ title, placement, children, ...args }: OverridableTooltipType) => React.JSX.Element;
export default Tooltip;
